import { index, pgTable, bigint, timestamp, text, uuid, unique } from "drizzle-orm/pg-core";
import { devicesTable } from "./nexora";
import { agentSigningKeysTable } from "./agent-signing";
export const agentRequestNoncesTable = pgTable("nexora_agent_request_nonces", { id: uuid("id").defaultRandom().primaryKey(), deviceId: uuid("device_id").notNull().references(() => devicesTable.id, { onDelete: "cascade" }), signingKeyId: uuid("signing_key_id").notNull().references(() => agentSigningKeysTable.id, { onDelete: "cascade" }), nonceHash: text("nonce_hash").notNull(), requestTimestamp: bigint("request_timestamp", { mode: "number" }).notNull(), expiresAt: timestamp("expires_at", { withTimezone: true }).notNull(), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() }, (t) => [unique("nexora_agent_nonce_unique").on(t.deviceId, t.nonceHash), index("nexora_agent_nonce_expiry_idx").on(t.expiresAt), index("nexora_agent_nonce_device_idx").on(t.deviceId)]);
